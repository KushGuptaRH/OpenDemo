---
- name: This is a demo play
  hosts: localhost
  become: true
  tasks:
    - name: this is a name filler
      copy:
        content: |
          Lets make a change
        dest: /home/testfile.txt
    - name: file lookup demo
      debug:
        msg: "{{ lookup('file', '/home/testfile.txt') }}"
